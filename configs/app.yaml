app:
  name: book-qa-library
  version: 0.1.0
  env: local
  host: 127.0.0.1
  port: 8000
  debug: false
  log_level: INFO
  request_id_header: X-Request-ID

storage:
  database_url: sqlite:///./data/app.db
  data_dir: ./data

models:
  profile: local
